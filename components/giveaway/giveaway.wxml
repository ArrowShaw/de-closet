<!--components/giveaway/giveaway.wxml-->
<view class="container">
  <view class="category-bar" wx:for="{{giveaway.items}}">
    <view wx:for_item="{{item}}" wx:if="{{item.is_giveaway}} == true">
      <image class="photo_container" src="{{item.photo}}"></image>
    </view>
  </view>
</view>

