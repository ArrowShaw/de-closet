<!--pages/giveaways/giveaways.wxml-->

<navbar home text='Giveaways' />

  <view class="text">You have {{giveaways.items.length}} {{itemName}} in your giveaways 💛</view>
    <block class="container" wx:for="{{giveaways.items}}" wx:key="value">
      <view color="#F2CE44" class="category-bar" wx:for_item="{{item}}" value="{{item.id}}" checked="{{item.checked}}">
        <image class="photo_container" src="{{item.photo}}"></image>
      </view>
    </block>

<button data-user_id="{{currentUser.id}}" open-type="share">Share</button>

<tabbar activeIndex="{{1}}"/>